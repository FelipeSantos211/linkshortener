# ==============================================
# EXEMPLO DE VARIÁVEIS DE AMBIENTE
# ==============================================
# Copie este arquivo para .env e ajuste os valores conforme necessário
# IMPORTANTE: Nunca commite o arquivo .env com credenciais reais!

# ==============================================
# BANCO DE DADOS
# ==============================================
# Nome do banco de dados
DATABASE_NAME=linkshortener

# URL de conexão com PostgreSQL
# Desenvolvimento local: jdbc:postgresql://localhost:5432/linkshortener
# Docker Compose: jdbc:postgresql://db:5432/linkshortener
DATABASE_URL=jdbc:postgresql://db:5432/linkshortener

# Credenciais do banco de dados
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=maneger

# Porta do PostgreSQL (apenas para desenvolvimento local)
DATABASE_PORT=5432

# ==============================================
# JWT (JSON Web Token)
# ==============================================
# Chave secreta para assinatura de tokens JWT
# ATENÇÃO: Use uma chave forte e única em produção!
# Exemplo de geração: openssl rand -base64 64
JWT_SECRET=SUA_CHAVE_SECRETA_MUITO_GRANDE_E_SEGURA_AQUI_123456

# Tempo de expiração do token em horas
JWT_EXPIRATION_HOURS=1

# ==============================================
# APLICAÇÃO
# ==============================================
# URL base pública da aplicação (usada para gerar links curtos completos)
# Desenvolvimento: http://localhost:8080
# Produção: https://seudominio.com
APP_BASE_URL=http://localhost:8080

# Porta do servidor
SERVER_PORT=8080

# ==============================================
# JPA/HIBERNATE (OPCIONAL)
# ==============================================
# Mostrar SQL no console (true em dev, false em prod)
JPA_SHOW_SQL=true

# Estratégia de criação do schema
# Valores: create, create-drop, update, validate, none
# Produção: validate ou none
JPA_DDL_AUTO=update
